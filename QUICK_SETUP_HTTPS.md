# 🚀 Быстрая настройка HTTPS на порту 8443

## ✅ Что уже сделано:

1. ✅ Обновлён `appsettings.json` - добавлена конфигурация HTTPS и сертификата
2. ✅ Обновлён `Program.cs` - исправлена логика для работы HTTPS в любом режиме
3. ✅ Созданы скрипты для генерации сертификата

## 📋 Что нужно сделать на сервере:

### 1. Создать сертификат на сервере

**Вариант A: Используя скрипт (рекомендуется)**
```bash
# Скопируйте скрипт на сервер
scp create_certificate.sh user@5.59.232.211:/tmp/

# На сервере выполните
ssh user@5.59.232.211
sudo chmod +x /tmp/create_certificate.sh
sudo /tmp/create_certificate.sh
```

**Вариант B: Вручную**
```bash
# Создать директории
sudo mkdir -p /etc/ssl/certs /etc/ssl/private

# Создать сертификат и ключ
sudo openssl req -x509 -newkey rsa:4096 \
    -keyout /etc/ssl/private/yess-cert-key.pem \
    -out /etc/ssl/certs/yess-cert.pem \
    -days 365 -nodes \
    -subj "/CN=5.59.232.211/O=Yess Loyalty/C=KG" \
    -addext "subjectAltName=IP:5.59.232.211"

# Преобразовать в PFX формат
sudo openssl pkcs12 -export \
    -out /etc/ssl/certs/yess-cert.pfx \
    -inkey /etc/ssl/private/yess-cert-key.pem \
    -in /etc/ssl/certs/yess-cert.pem \
    -passout pass:"YesSGo!@#!"

# Установить права доступа
sudo chmod 644 /etc/ssl/certs/yess-cert.pfx
sudo chmod 600 /etc/ssl/private/yess-cert-key.pem
```

### 2. Убедиться, что firewall открыт
```bash
sudo ufw allow 8443/tcp
sudo ufw status
```

### 3. Перезапустить приложение
```bash
# Если systemd service
sudo systemctl restart yess-backend
sudo systemctl status yess-backend

# Если Docker
docker-compose restart

# Если запущено вручную
# Остановите (Ctrl+C) и запустите заново
```

### 4. Проверить, что порт слушается
```bash
sudo netstat -tlnp | grep 8443
# или
sudo ss -tlnp | grep 8443

# Должно быть что-то вроде:
# tcp  0  0  0.0.0.0:8443  0.0.0.0:*  LISTEN  <pid>/dotnet
```

### 5. Проверить HTTPS подключение
```bash
curl -vk https://5.59.232.211:8443
# или
curl -k https://5.59.232.211:8443/health
```

## 🔍 Проверка логов

Посмотрите логи приложения - должно быть сообщение:
```
Now listening on: https://0.0.0.0:8443
```

## ⚙️ Текущая конфигурация

- **Порт HTTPS:** 8443
- **Путь к сертификату:** `/etc/ssl/certs/yess-cert.pfx`
- **Пароль сертификата:** `YesSGo!@#!`
- **IP адрес:** 5.59.232.211

## ⚠️ Важные примечания

1. Если сертификат находится в другом месте, обновите путь в `appsettings.json`:
   ```json
   "Kestrel": {
     "Certificates": {
       "Default": {
         "Path": "/путь/к/вашему/сертификату.pfx",
         "Password": "YesSGo!@#!"
       }
     }
   }
   ```

2. Самоподписанный сертификат подходит только для тестирования. Для production рекомендуется использовать Let's Encrypt.

3. Если приложение работает в Docker, убедитесь, что сертификат доступен внутри контейнера (volume mount).

## ❌ Решение проблем

### Connection refused
- Проверьте, что приложение запущено
- Проверьте firewall: `sudo ufw status`
- Проверьте, что порт слушается: `sudo netstat -tlnp | grep 8443`

### Сертификат не найден
- Проверьте путь в `appsettings.json`
- Убедитесь, что файл существует: `ls -la /etc/ssl/certs/yess-cert.pfx`
- Проверьте права доступа: `sudo chmod 644 /etc/ssl/certs/yess-cert.pfx`

### Ошибка при загрузке сертификата
- Проверьте пароль в `appsettings.json`
- Убедитесь, что формат файла правильный (PFX)
- Проверьте логи приложения на детали ошибки

