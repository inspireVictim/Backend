# 📬 Настройка Postman для тестирования API

## 📥 Импорт файлов

1. Откройте **Postman**
2. Нажмите **Import** (кнопка в левом верхнем углу)
3. Перетащите файлы или выберите через **File**:
   - `YessBackend_API.postman_collection.json` - коллекция запросов
   - `YessBackend_API.postman_environment.json` - переменные окружения (опционально)

## ⚙️ Настройка переменных окружения

### Способ 1: Использование файла Environment

1. После импорта файла Environment выберите его в выпадающем списке в правом верхнем углу
2. Убедитесь, что выбрана среда "Yess Backend - Production"
3. При необходимости измените переменные:
   - `base_url` - URL сервера (по умолчанию: `https://5.59.232.211:8443`)
   - `user_phone` - ваш номер телефона для тестирования
   - `user_password` - ваш пароль

### Способ 2: Настройка переменных в коллекции

Если не используете файл Environment, переменные можно настроить прямо в коллекции:

1. Откройте коллекцию "Yess Backend API"
2. Перейдите на вкладку **Variables**
3. Отредактируйте переменные:
   - `base_url`: `https://5.59.232.211:8443`
   - Остальные переменные заполнятся автоматически при выполнении запросов

## 🔐 Автоматическое сохранение токенов

Коллекция настроена так, что после успешного входа (`Login`) токены автоматически сохраняются в переменные:
- `access_token` - токен доступа
- `refresh_token` - токен обновления

Эти токены автоматически подставляются в заголовок `Authorization: Bearer {{access_token}}` для всех запросов, требующих аутентификации.

## 📋 Структура коллекции

### Health
- ✅ **Health Check** - Проверка работоспособности API

### Authentication
- 🔐 **Register** - Регистрация нового пользователя
- 🔐 **Login** - Вход (автоматически сохраняет токены)
- 🔄 **Refresh Token** - Обновление токена
- 👤 **Get Me** - Информация о текущем пользователе
- 📱 **Send Verification Code** - Отправка SMS кода
- ✅ **Verify Code** - Верификация SMS кода

### Partners
- 📋 **Get Partners List** - Список партнеров (с фильтрами)
- 🔍 **Get Partner by ID** - Информация о партнере

### Wallet
- 💰 **Get Wallet** - Информация о кошельке
- 💵 **Get Balance** - Баланс кошелька
- 📜 **Get Transactions** - История транзакций

### Orders
- 🧮 **Calculate Order** - Расчет стоимости заказа
- 🛒 **Create Order** - Создание заказа
- 📦 **Get Order by ID** - Информация о заказе
- 📋 **Get My Orders** - Список моих заказов
- 💳 **Pay Order** - Оплата заказа

### QR Code
- 📱 **Generate QR Code** - Генерация QR кода для оплаты
- 📷 **Scan QR Code** - Сканирование QR кода

### Promotions
- 🎁 **Get Promotions** - Список акций и промо

### Banners
- 🖼️ **Get Banners** - Список баннеров

### Achievements
- 🏆 **Get Achievements** - Список достижений пользователя

### Notifications
- 🔔 **Get Notifications** - Список уведомлений
- ✅ **Mark Notification as Read** - Отметить уведомление как прочитанное

### Routes
- 🗺️ **Get Routes** - Построение маршрута

### Locations
- 📍 **Search Locations** - Поиск локаций

### Upload
- 📤 **Upload File** - Загрузка файла

### Payments
- 💳 **Get Payment Methods** - Список методов оплаты
- 💰 **Initiate Payment** - Инициирование платежа

### Admin
- 👨‍💼 **Admin Login** - Вход администратора

### Webhooks
- 🔗 **Optima Webhook** - Webhook для Optima платежей

## 🚀 Быстрый старт

1. **Импортируйте коллекцию и environment файлы**

2. **Выберите Environment**: "Yess Backend - Production"

3. **Проверьте Health**:
   - Откройте запрос `Health > Health Check`
   - Нажмите **Send**
   - Должен вернуться статус `200 OK`

4. **Зарегистрируйтесь** (если еще нет аккаунта):
   - Откройте `Authentication > Register`
   - Измените `phone` и `password` в теле запроса
   - Нажмите **Send**

5. **Войдите в систему**:
   - Откройте `Authentication > Login`
   - Убедитесь, что `phone` и `password` соответствуют зарегистрированным
   - Нажмите **Send**
   - Токены автоматически сохранятся в переменные

6. **Тестируйте защищенные эндпоинты**:
   - Теперь вы можете вызывать любые запросы, требующие авторизации
   - Токен автоматически подставляется из переменной `{{access_token}}`

## 🔧 Настройка SSL сертификата (если нужно)

Если Postman показывает ошибку SSL сертификата:

1. Откройте **Settings** (⚙️) в Postman
2. Перейдите в **General** → **SSL certificate verification**
3. Для тестирования можно временно отключить проверку SSL (только для разработки!)
4. Или добавьте сертификат в **Settings** → **Certificates**

## 📝 Примеры использования

### Пример 1: Получить список партнеров

1. Откройте `Partners > Get Partners List`
2. При необходимости добавьте фильтры в Query параметры:
   - `city_id` - ID города
   - `category` - Категория
   - `latitude`, `longitude`, `radius_km` - Геолокация
3. Нажмите **Send**

### Пример 2: Создать заказ

1. Сначала выполните `Login` для получения токена
2. Откройте `Orders > Create Order`
3. Измените JSON в теле запроса:
   ```json
   {
       "partner_id": 1,
       "items": [
           {
               "product_id": 1,
               "quantity": 2
           }
       ],
       "delivery_address": "г. Бишкек, ул. Чуйская, 123"
   }
   ```
4. Нажмите **Send**

### Пример 3: Получить баланс кошелька

1. Убедитесь, что вы выполнили `Login`
2. Откройте `Wallet > Get Balance`
3. Нажмите **Send**
4. Должен вернуться баланс вашего кошелька

## 🐛 Решение проблем

### Проблема: "Connection refused"

**Решение:**
- Проверьте, что сервер запущен: `curl https://5.59.232.211:8443/api/v1/health`
- Проверьте переменную `base_url` в Postman

### Проблема: "401 Unauthorized"

**Решение:**
- Выполните запрос `Login` снова для получения нового токена
- Проверьте, что токен автоматически сохранился в переменной `access_token`
- Убедитесь, что в заголовке запроса есть `Authorization: Bearer {{access_token}}`

### Проблема: "SSL certificate error"

**Решение:**
- Временно отключите проверку SSL в Settings → General → SSL certificate verification
- Или добавьте сертификат в Settings → Certificates

### Проблема: Токены не сохраняются автоматически

**Решение:**
- Убедитесь, что запрос `Login` выполнился успешно (код 200)
- Проверьте вкладку **Tests** запроса Login - там должен быть скрипт сохранения токенов
- Если скрипта нет, добавьте его из коллекции

## 📚 Дополнительная информация

- Базовый URL API: `https://5.59.232.211:8443`
- Версия API: `v1`
- Все эндпоинты начинаются с: `/api/v1/`
- Swagger документация: `https://5.59.232.211:8443/swagger` (если включен)

## ✅ Готово!

Теперь вы можете начать тестировать API в Postman. Начните с Health Check, затем зарегистрируйтесь, войдите и тестируйте остальные эндпоинты!

