# Автоматическое применение миграций

## ✅ Реализовано

Backend автоматически применяет миграции базы данных при запуске приложения.

## 🔧 Настройка

### Включение/отключение автоматических миграций

В файле `appsettings.json`:

```json
{
  "Database": {
    "AutoMigrate": true,
    "MigrationTimeoutSeconds": 60
  }
}
```

**Параметры:**
- `AutoMigrate` (bool, по умолчанию: `true`) - включить/отключить автоматическое применение миграций
- `MigrationTimeoutSeconds` (int, по умолчанию: `60`) - таймаут применения миграций в секундах

## 📋 Как это работает

1. **При старте приложения:**
   - Проверяется подключение к базе данных
   - Получается список ожидающих миграций
   - Применяются все ожидающие миграции автоматически
   - Логируется процесс применения

2. **Логирование:**
   - ✅ Информация о количестве миграций
   - ✅ Список примененных миграций
   - ✅ Статус завершения

3. **Обработка ошибок:**
   - В Development режиме: ошибки приводят к остановке приложения
   - В Production режиме: ошибки логируются, но приложение продолжает работу

## 🚀 Использование

### Автоматическое применение (по умолчанию)

Просто запустите приложение - миграции применятся автоматически:

```bash
dotnet run
# или
docker-compose up
```

### Отключение автоматического применения

Если нужно применить миграции вручную, установите:

```json
{
  "Database": {
    "AutoMigrate": false
  }
}
```

Затем примените миграции вручную:

```bash
dotnet ef database update --project YessBackend.Infrastructure --startup-project YessBackend.Api
```

## 📝 Примеры логов

### Успешное применение миграций

```
info: Проверка подключения к базе данных...
info: Подключение к базе данных установлено.
info: Текущие миграции в БД: 1
info: Найдено 2 ожидающих миграций:
info:   → 20241125120000_AddPaymentProviderTransactions
info:   → 20241125130000_UpdateOptimaIntegration
info: Применение миграций (таймаут: 60 секунд)...
info: ✅ Все миграции успешно применены.
info: ✅ База данных полностью актуальна.
```

### Нет ожидающих миграций

```
info: Проверка подключения к базе данных...
info: Подключение к базе данных установлено.
info: Текущие миграции в БД: 3
info: ✅ База данных актуальна, ожидающих миграций нет.
```

### Отключено в конфигурации

```
info: Автоматическое применение миграций отключено в конфигурации.
```

## ⚠️ Важные замечания

1. **В Production:** Убедитесь, что `AutoMigrate` включен или примените миграции вручную перед запуском
2. **Таймаут:** Если миграции очень большие, увеличьте `MigrationTimeoutSeconds`
3. **Резервное копирование:** Рекомендуется создавать резервную копию БД перед применением миграций в Production

## 🔍 Проверка состояния миграций

Проверить, какие миграции применены:

```bash
docker exec -it yess-postgres psql -U yess_user -d yess_db -c "SELECT * FROM \"__EFMigrationsHistory\" ORDER BY \"MigrationId\";"
```

Или через .NET CLI:

```bash
dotnet ef migrations list --project YessBackend.Infrastructure --startup-project YessBackend.Api
```

