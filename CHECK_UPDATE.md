# ✅ Проверка применения изменений на сервере

## ⚠️ ВАЖНО: Изменения кода требуют пересборки Docker образа!

Когда вы делаете `docker-compose up` **без флага `--build`**, Docker может использовать **старый кешированный образ**, и ваши изменения кода **НЕ применятся**.

## 🔧 Правильная команда для обновления:

```bash
sudo docker-compose down
git pull
sudo docker-compose up -d --build
```

**Ключевое отличие:** флаг `--build` **принудительно пересобирает** Docker образ с новым кодом.

## 🚀 Или используйте готовый скрипт:

```bash
chmod +x UPDATE_SERVER.sh
./UPDATE_SERVER.sh
```

## 🔍 Как проверить, что изменения применились:

### 1. Проверка логов на наличие ошибок

```bash
sudo docker-compose logs --tail=100 csharp-backend
```

### 2. Проверка, что новый код работает

Проверьте эндпоинт Optima Payment с новыми валидациями:

```bash
# Должна вернуться ошибка валидации txn_id (если формат неверный)
curl "http://localhost:8000/api/v1/optima/payment?command=check&txn_id=abc&account=1&sum=10.45"
```

Если изменения применились, вы увидите XML ответ с ошибкой валидации.

### 3. Проверка через Swagger

Откройте в браузере:
```
http://5.59.232.211:8000/swagger
```

Найдите эндпоинт `GET /api/v1/optima/payment` и проверьте его описание.

### 4. Проверка кода внутри контейнера (опционально)

```bash
# Войти в контейнер
sudo docker exec -it csharp-backend bash

# Проверить содержимое файла (например, контроллера)
cat /app/YessBackend.Api.dll  # Это скомпилированный код
```

### 5. Проверка версии через health check

```bash
curl http://localhost:8000/api/v1/health
```

## 📋 Что было изменено:

1. ✅ Валидация `txn_id` (до 20 цифр, только цифры)
2. ✅ Валидация `account` (1-10 цифр, регулярное выражение)
3. ✅ Валидация `sum` (формат 10.45, строгий regex)
4. ✅ Улучшенная IP-фильтрация
5. ✅ Правильный порядок элементов XML ответа
6. ✅ Сохранение всех транзакций в `PaymentProviderTransactions`
7. ✅ Улучшенная обработка идемпотентности

## 🔄 Если изменения НЕ применились:

1. **Остановите все контейнеры:**
   ```bash
   sudo docker-compose down
   ```

2. **Удалите старый образ (опционально, для полной очистки):**
   ```bash
   sudo docker rmi yess-backend-dotnet_csharp-backend
   ```

3. **Пересоберите и запустите:**
   ```bash
   sudo docker-compose up -d --build --force-recreate
   ```

4. **Проверьте логи:**
   ```bash
   sudo docker-compose logs -f csharp-backend
   ```

## ✅ Признаки успешного применения:

- Логи показывают запуск без ошибок
- API отвечает на запросы
- Валидация работает (ошибки при неверных форматах)
- XML ответы содержат правильный порядок элементов
- Таблица `PaymentProviderTransactions` создается (если миграции применены)

## 🐛 Отладка:

Если что-то не работает:

1. **Проверьте статус контейнеров:**
   ```bash
   sudo docker-compose ps
   ```

2. **Проверьте логи всех сервисов:**
   ```bash
   sudo docker-compose logs
   ```

3. **Проверьте подключение к базе:**
   ```bash
   sudo docker exec -it yess-postgres psql -U yess_user -d yess_db -c "SELECT COUNT(*) FROM \"PaymentProviderTransactions\";"
   ```

4. **Перезапустите с пересборкой:**
   ```bash
   sudo docker-compose up -d --build --force-recreate
   ```

