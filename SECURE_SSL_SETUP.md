# 🔐 Безопасная настройка SSL сертификата для HTTPS

## 📋 Обзор

Этот документ описывает рекомендуемый безопасный способ настройки SSL сертификата для HTTPS на порту 8443. Используется комбинация:
- ✅ Создание сертификата на сервере при деплое
- ✅ Использование переменных окружения для пароля
- ✅ Исключение сертификатов из git репозитория

## 🎯 Преимущества этого подхода

1. **Безопасность**: Пароль и приватный ключ не попадают в git репозиторий
2. **Гибкость**: Можно использовать разные сертификаты на разных окружениях
3. **Простота**: Автоматическое создание сертификата при первом деплое
4. **Соответствие best practices**: Соответствует рекомендациям по безопасности

---

## 📝 Шаг 1: Первоначальная настройка (один раз)

### 1.1 Обновите .gitignore

Убедитесь, что в `.gitignore` есть следующие правила (уже добавлены):

```
*.pfx
*.pem
*.key
*.crt
*.cer
certs/
ssl/
appsettings.Production.json
```

### 1.2 Настройка кода

Код уже обновлён для поддержки переменных окружения:
- `Program.cs` читает путь и пароль из переменных окружения `SSL_CERT_PATH` и `SSL_CERT_PASSWORD`
- Если переменные не установлены, используются значения из `appsettings.json`

---

## 🚀 Шаг 2: Настройка на сервере

### 2.1 Создание сертификата на сервере

**Вариант A: Автоматическое создание при деплое (рекомендуется)**

Добавьте в скрипт деплоя или CI/CD:

```bash
#!/bin/bash
# deploy.sh или в CI/CD pipeline

CERT_PATH="/etc/ssl/certs/yess-cert.pfx"

# Создаём сертификат, если его ещё нет
if [ ! -f "$CERT_PATH" ]; then
    echo "📝 Создание SSL сертификата..."
    
    # Создаём директории
    sudo mkdir -p /etc/ssl/certs /etc/ssl/private
    
    # Создаём сертификат
    sudo openssl req -x509 -newkey rsa:4096 \
        -keyout /etc/ssl/private/yess-cert-key.pem \
        -out /etc/ssl/certs/yess-cert.pem \
        -days 365 -nodes \
        -subj "/CN=5.59.232.211/O=Yess Loyalty/C=KG" \
        -addext "subjectAltName=IP:5.59.232.211"
    
    # Преобразуем в PFX
    sudo openssl pkcs12 -export \
        -out "$CERT_PATH" \
        -inkey /etc/ssl/private/yess-cert-key.pem \
        -in /etc/ssl/certs/yess-cert.pem \
        -passout pass:"YesSGo!@#!" \
        -name "Yess Backend Certificate"
    
    # Устанавливаем права доступа
    sudo chmod 644 "$CERT_PATH"
    sudo chmod 600 /etc/ssl/private/yess-cert-key.pem
    
    echo "✅ Сертификат создан: $CERT_PATH"
else
    echo "✅ Сертификат уже существует: $CERT_PATH"
fi
```

**Вариант B: Ручное создание**

Выполните на сервере:

```bash
# Скопируйте скрипт create_certificate.sh на сервер
scp create_certificate.sh user@5.59.232.211:/tmp/

# На сервере
ssh user@5.59.232.211
sudo chmod +x /tmp/create_certificate.sh
sudo /tmp/create_certificate.sh
```

### 2.2 Настройка переменных окружения

**Для systemd service:**

Создайте или отредактируйте файл `/etc/systemd/system/yess-backend.service`:

```ini
[Unit]
Description=Yess Backend API
After=network.target

[Service]
Type=notify
ExecStart=/usr/bin/dotnet /opt/yess-backend/YessBackend.Api.dll
Restart=always
RestartSec=10
SyslogIdentifier=yess-backend
User=www-data
Environment=ASPNETCORE_ENVIRONMENT=Production
Environment=SSL_CERT_PATH=/etc/ssl/certs/yess-cert.pfx
Environment=SSL_CERT_PASSWORD=YesSGo!@#!

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl restart yess-backend
```

**Для Docker / Docker Compose:**

Обновите `docker-compose.yml`:

```yaml
services:
  api:
    image: yess-backend:latest
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - SSL_CERT_PATH=/etc/ssl/certs/yess-cert.pfx
      - SSL_CERT_PASSWORD=YesSGo!@#!
    volumes:
      - /etc/ssl/certs:/etc/ssl/certs:ro
    ports:
      - "8443:8443"
```

**Для .env файла (если используется):**

Создайте `.env` файл на сервере (не коммитьте в git):

```bash
# .env (на сервере, не в git)
ASPNETCORE_ENVIRONMENT=Production
SSL_CERT_PATH=/etc/ssl/certs/yess-cert.pfx
SSL_CERT_PASSWORD=YesSGo!@#!
```

### 2.3 Альтернатива: Использование appsettings.Production.json

Если вы предпочитаете использовать файл конфигурации:

1. На сервере создайте `appsettings.Production.json`:

```json
{
  "Kestrel": {
    "Certificates": {
      "Default": {
        "Path": "/etc/ssl/certs/yess-cert.pfx",
        "Password": "YesSGo!@#!"
      }
    }
  }
}
```

2. Убедитесь, что файл не попадёт в git (уже в `.gitignore`)

3. Установите переменную окружения:
```bash
export ASPNETCORE_ENVIRONMENT=Production
```

---

## 🔍 Шаг 3: Проверка

### 3.1 Проверка файла сертификата

```bash
# Проверьте, что файл существует
ls -la /etc/ssl/certs/yess-cert.pfx

# Проверьте права доступа
# Должно быть: -rw-r--r-- (644)
```

### 3.2 Проверка переменных окружения

```bash
# Если используете systemd
sudo systemctl show yess-backend | grep Environment

# Если используете Docker
docker exec <container_name> env | grep SSL

# Если запускаете вручную
env | grep SSL
```

### 3.3 Проверка, что порт слушается

```bash
sudo netstat -tlnp | grep 8443
# или
sudo ss -tlnp | grep 8443
```

### 3.4 Тестирование HTTPS подключения

```bash
# С сервера
curl -vk https://localhost:8443

# С другого компьютера
curl -vk https://5.59.232.211:8443

# Проверка health endpoint
curl -k https://5.59.232.211:8443/health
```

### 3.5 Проверка логов

```bash
# Если systemd
sudo journalctl -u yess-backend -f

# Должно быть сообщение:
# Now listening on: https://0.0.0.0:8443
```

---

## 🔄 Шаг 4: Обновление приложения

После каждого деплоя:

1. **Убедитесь, что сертификат существует** (если используете автоматическое создание, это не нужно)
2. **Проверьте переменные окружения** - они должны быть установлены
3. **Перезапустите приложение**:

```bash
# systemd
sudo systemctl restart yess-backend

# Docker
docker-compose restart

# Вручную
# Остановите и запустите заново
```

---

## 🔐 Безопасность

### ✅ Что сделано правильно:

1. ✅ Сертификаты и приватные ключи исключены из git
2. ✅ Пароль не хранится в `appsettings.json` (только в переменных окружения)
3. ✅ Путь к сертификату можно переопределить через переменную окружения
4. ✅ Сертификат создаётся на сервере, не копируется из репозитория

### ⚠️ Дополнительные рекомендации:

1. **Используйте сильный пароль** для сертификата (не храните в коде)
2. **Регулярно обновляйте сертификат** (перед истечением срока действия)
3. **Для production**: Рассмотрите использование Let's Encrypt вместо самоподписанного сертификата
4. **Ограничьте доступ** к файлу сертификата (только для пользователя приложения)
5. **Используйте secrets management** (HashiCorp Vault, Azure Key Vault, AWS Secrets Manager) для хранения паролей в enterprise окружениях

---

## 📋 Чеклист деплоя

- [ ] Сертификат создан на сервере (`/etc/ssl/certs/yess-cert.pfx`)
- [ ] Переменные окружения установлены (`SSL_CERT_PATH`, `SSL_CERT_PASSWORD`)
- [ ] Firewall настроен (порт 8443 открыт)
- [ ] Приложение перезапущено
- [ ] Порт 8443 слушается (`sudo netstat -tlnp | grep 8443`)
- [ ] HTTPS работает (`curl -vk https://5.59.232.211:8443`)

---

## 🆘 Решение проблем

### Проблема: Connection refused

**Причина**: Приложение не запущено или порт не слушается

**Решение**:
```bash
# Проверьте статус приложения
sudo systemctl status yess-backend

# Проверьте логи
sudo journalctl -u yess-backend -n 50

# Проверьте, что порт слушается
sudo netstat -tlnp | grep 8443
```

### Проблема: Сертификат не найден

**Причина**: Неправильный путь к сертификату

**Решение**:
```bash
# Проверьте путь
ls -la /etc/ssl/certs/yess-cert.pfx

# Проверьте переменную окружения
echo $SSL_CERT_PATH

# Проверьте права доступа
sudo chmod 644 /etc/ssl/certs/yess-cert.pfx
```

### Проблема: Ошибка при загрузке сертификата

**Причина**: Неправильный пароль или повреждённый файл

**Решение**:
```bash
# Проверьте пароль в переменной окружения
echo $SSL_CERT_PASSWORD

# Проверьте формат файла
file /etc/ssl/certs/yess-cert.pfx
# Должно быть: PKCS12

# Пересоздайте сертификат, если нужно
sudo rm /etc/ssl/certs/yess-cert.pfx
sudo ./create_certificate.sh
```

### Проблема: Приложение не читает переменные окружения

**Причина**: Переменные не установлены или приложение запущено до их установки

**Решение**:
```bash
# Установите переменные в systemd service
sudo systemctl edit yess-backend

# Добавьте:
[Service]
Environment="SSL_CERT_PATH=/etc/ssl/certs/yess-cert.pfx"
Environment="SSL_CERT_PASSWORD=YesSGo!@#!"

# Перезагрузите systemd и перезапустите сервис
sudo systemctl daemon-reload
sudo systemctl restart yess-backend
```

---

## 📚 Дополнительные ресурсы

- [ASP.NET Core HTTPS Configuration](https://docs.microsoft.com/en-us/aspnet/core/security/enforcing-ssl)
- [Kestrel HTTPS Configuration](https://docs.microsoft.com/en-us/aspnet/core/fundamentals/servers/kestrel/endpoints)
- [OpenSSL Certificate Generation](https://www.openssl.org/docs/man1.1.1/man1/openssl-req.html)

---

## ✅ Итоговая структура

```
На сервере:
├── /etc/ssl/certs/yess-cert.pfx          # Сертификат (не в git)
├── /etc/ssl/private/yess-cert-key.pem    # Приватный ключ (не в git)
└── Переменные окружения:
    ├── SSL_CERT_PATH=/etc/ssl/certs/yess-cert.pfx
    └── SSL_CERT_PASSWORD=YesSGo!@#!

В git репозитории:
├── Program.cs                             # ✅ Обновлён для поддержки env vars
├── appsettings.json                       # ✅ Без пароля
├── appsettings.Production.json.example    # ✅ Шаблон
├── create_certificate.sh                  # ✅ Скрипт создания
├── .gitignore                             # ✅ Исключает сертификаты
└── SECURE_SSL_SETUP.md                    # ✅ Эта инструкция
```

---

**Готово! Теперь у вас безопасная настройка SSL сертификата.** 🎉

